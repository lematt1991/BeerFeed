// Test Cases for PolicyMenu Component

import React from 'react';
import {expect} from 'chai';
import {mount, render} from 'enzyme';

describe('<PolicyMenu/>', () => {
	it('Mount component', () => {
		const wrapper = render(
			<div></div>
		)	
	}) 
	expect(true)
})

